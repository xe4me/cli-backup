"use strict";
classifiedModuleName %  > ;
from;
'./<%= fileName %>';
describe('<%= classifiedModuleName %>', function () {
    it('should create an instance', function () {
        expect(new  < , classifiedModuleName %  > ()).toBeTruthy();
    });
});
