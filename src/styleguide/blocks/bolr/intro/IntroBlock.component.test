@@ set
    task    bolr-intro-block
    url     http://localhost:3001/#/components/BOLRIntrolBlock

@@ parameterized
    | subtask    | spec                                                      |
    | All        | src/styleguide/blocks/bolr/intro/IntroBlock.component.test.spec    |

@@ parameterized
    | browser    |
    | phantomjs  |

@@ parameterized
    | breakpoint | tags    | size        |
    | Desk       | desk    | 1025x800    |
    | Laptop     | laptop  | 1024x800    |
    | Tablet     | tablet  | 767x800     |
    | Palm       | palm    | 480x800     |
    | Mobile     | mobile  | 320x800     |


@@ groups ddc, bolr-intro-block
${task}: ${subtask} - ${breakpoint}
    selenium ${browser} ${url} ${size}
        wait 5s until gone "css: .ng-cloak"
        wait 5s until exist "css: .bolr-intro"
        check ${spec} --include "${tags}"
