@@ set
    task    AmpSwitch
    url     http://localhost:3001/#/components/AmpSwitchComponent

@@ parameterized
    | subtask    | spec                                                      |
    | All        | src/styleguide/components/amp-switch/amp-switch.test.spec    |

@@ parameterized
    | browser    |
    | chrome  |

@@ parameterized
    | breakpoint | tags    | size        |
    | Desk       | desk    | 1025x800    |


@@ groups ddc, my-md-input
${task}: ${subtask} - ${breakpoint}
    selenium ${browser} ${url} ${size}
        wait 1s
        check ${spec} --include "${tags}"
