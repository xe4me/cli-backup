@@ set
    task    MyMdInput
    url     http://localhost:3001/#/components/AmpLinearProgressBarComponent

@@ parameterized
    | subtask    | spec                                                      |
    | All        | ./amp-linear-progress-bar/amp-linear-progress-bar.test.spec    |

@@ parameterized
    | browser    |
    | chrome  |

@@ parameterized
    | breakpoint | tags    | size        |
    | Desk       | desk    | 1025x800    |


@@ groups ddc, my-md-input
${task}: ${subtask} - ${breakpoint}
    selenium ${browser} ${url} ${size}
        wait 5s
        check ${spec} --include "${tags}"
